<!DOCTYPE html>
<html>

  <head>

    <!-- CORE -->
    <script src="__JS_URL__"></script>


  </head>

  <body>

    <!-- your content goes here -->

    <!-- APP -->
    <script type="text/javascript">
      var main = function() {
		var app = EThing.auth.getApp();
		
		var title = document.createElement("H1");
		title.appendChild(document.createTextNode(app.name()));
        document.body.appendChild(title);
		
		var description = document.createElement("P");
		description.appendChild(document.createTextNode('This is a very basic app example'));
		document.body.appendChild(description);
		
      };

      EThing.initialize({
        serverUrl: '__SERVER_URL__',
        apiKey: '__API_KEY__'
      }, main || null, function(error) {
        // on error
        alert(error.message);
      });
      
    </script>

  </body>

</html>